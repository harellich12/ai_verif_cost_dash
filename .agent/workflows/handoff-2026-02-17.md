# Handoff (2026-02-17)

## Completed fixes (all prior "Bad" items)

1. `computeMode` now drives ROI cost path (API vs self-hosted) in calculations.
2. Removed CapEx double-counting in annual totals/break-even.
3. VaaS monthly timeline/cost logic fixed:
   - hiring lag applied to internal progress
   - horizon uses max(internal total duration, vaas duration)
   - VaaS cost accrues to quote over delivery period, then flatlines
4. Stale tests updated for current defaults/logic and new behavior checks added.
5. PDF export paginates multi-page output instead of clipping.
6. Numeric input hardening added in hooks (NaN/infinite guard + clamping to config bounds).

## Main files changed

- `src/hooks/useROICalculator.ts`
- `src/hooks/useVaaSEstimator.ts`
- `src/components/PrintReportBtn.tsx`
- `src/components/KPICards.tsx`
- `src/components/ExecutiveSummary.tsx`
- `src/components/ResultChart.tsx`
- `src/components/ExcelExportBtn.tsx`
- `src/constants.ts`
- `src/App.tsx`
- `src/hooks/__tests__/useROICalculator.test.ts`
- `src/hooks/__tests__/useVaaSEstimator.test.ts`

## Validation status

- `./node_modules/.bin/tsc -b` passes.
- `./node_modules/.bin/eslint . --max-warnings=0` passes.
- `./node_modules/.bin/vitest --run` blocked by missing optional Rollup native package:
  - `@rollup/rollup-linux-x64-gnu`

## Next recommended steps

1. Fix environment/deps for Vitest and run tests.
2. Address previously identified "Medium" issues (dynamic Tailwind classes, doc consistency, etc.).

